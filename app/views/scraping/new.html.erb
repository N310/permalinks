<h1>投稿記事のリンク一括取得</h1>
<p>WordPressでパーマリンクを変更したい場合など、投稿記事の全URLが必要なときにご利用ください。</p>
<p>URLのみのものと、リダイレクト設定の記述がセットになったものと２種類ダウンロードできます。</p>

<%= form_with url: get_url_path, local: true, class: "form" do |form| %>
  <div class="form-content">
    <label for="url" class="form-label">TOPページのURL</label>
    <%= form.text_field :url, id:"url", placeholder: "https://example.com", class: "form-input" %>
  </div>
  <div class="form-content">
    <label for="selector" class="form-label">投稿記事 aタグのセレクタ</label>
    <%= form.text_field :selector, id:"selector", placeholder: ".post-list a", class: "form-input" %>
  </div>
  <%= form.submit '一括取得', name: nil, class:'button' %>
<% end %>

<div>
  <% @path_data.each do |path|  %>
    <span>redirect 301 <%= path %> <%= @host %>/newURL</span><br>
  <% end %>
</div>
